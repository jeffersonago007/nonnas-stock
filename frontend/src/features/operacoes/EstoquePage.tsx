import { PagePlaceholder } from '@/components/PagePlaceholder';

export function EstoquePage() {
  return (
    <PagePlaceholder
      title="Estoque"
      description="Saldos por filial, lote e validade"
      task="T14 — operações"
    />
  );
}
