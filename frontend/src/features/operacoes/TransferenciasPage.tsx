import { PagePlaceholder } from '@/components/PagePlaceholder';

export function TransferenciasPage() {
  return (
    <PagePlaceholder
      title="Transferências"
      description="State machine de transferências entre filiais"
      task="T14 — operações"
    />
  );
}
