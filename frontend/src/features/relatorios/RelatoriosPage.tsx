import { PagePlaceholder } from '@/components/PagePlaceholder';

export function RelatoriosPage() {
  return (
    <PagePlaceholder
      title="Relatórios"
      description="Curva ABC, ruptura, vencimento e divergência"
      task="T14 — operações"
    />
  );
}
