spring:
  application:
    name: nonnas-stock-operations
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

nonnas:
  operations:
    ajuste:
      threshold-aprovacao: 50

logging:
  level:
    org.hibernate.SQL: WARN
    org.flywaydb: INFO
