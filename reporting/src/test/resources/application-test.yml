spring:
  flyway:
    enabled: true
  task:
    scheduling:
      enabled: false  # cron jobs ficam desabilitados nos testes — invocamos manualmente

logging:
  level:
    com.nonnas: INFO
    org.springframework: WARN
    io.zonky: INFO
